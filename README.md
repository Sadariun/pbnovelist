# PBN NOVELIST
A Fast PBN template constructor made for building well-optimized and SEO-correct websites

## Структура проекта

### Секции и варианты

Каждая секция может иметь несколько вариантов дизайна. Варианты хранятся в папках с названиями `ver1`, `ver2`, `var1`, `var2` и т.д.

### Названия вариантов в meta.json

Для удобства работы можно задать читаемые названия для каждого варианта. Для этого добавьте поле `"name"` в файл `meta.json`:

**Пример структуры:**
```
2.HERO/
  └── ver1/
      ├── index.html
      ├── meta.json  <-- Содержит поле "name": "Классический герой"
      └── style.css
```

**Пример meta.json с названием:**
```json
{
  "name": "Классический герой",
  "index": 2,
  "required": true,
  "components": ["style.css"]
}
```

Это название будет отображаться в выпадающем списке выбора вариантов вместо технического имени "ver1".

### Примеры названий в meta.json:

- `1.HEADER/ver1/meta.json` → `"name": "Шапка базовая"`
- `1.HEADER/ver2/meta.json` → `"name": "Шапка расширенная"`
- `6.FEATURES/var1/meta.json` → `"name": "Сетка возможностей"`
- `6.FEATURES/var2/meta.json` → `"name": "Список возможностей"`

### Поля в meta.json:

- **name** (опционально) - Читаемое название варианта для отображения в UI
- **index** (обязательно) - Порядковый номер секции на странице
- **required** (обязательно) - true/false, является ли секция обязательной
- **components** (обязательно) - Массив файлов CSS/JS для данной секции

## Использование

1. Откройте `BOTBOT.html` в браузере
2. Введите основные данные (домен, язык и т.д.)
3. Нажмите "Загрузить компоненты"
4. Выберите секции и их варианты:
   - **Вручную** - выберите нужные секции из списка
   - **Случайно** 🎲 - нажмите кнопку "Случайный сайт" для автоматической генерации
5. Нажмите "Создать сайт"
6. Скачайте готовый архив или посмотрите превью на весь экран

### Случайная генерация сайта

Функция "🎲 Случайный сайт" автоматически:
- Позволяет выбрать желаемое количество секций (минимум 9)
- Выбирает случайные варианты для всех обязательных секций
- Добавляет случайные необязательные секции до указанного количества
- Для секций с вариантами выбирает случайную версию
- Валидирует минимальное количество секций
- Идеально подходит для быстрого прототипирования или вдохновения

## Особенности

✨ **Случайная генерация сайта** 🎲 - автоматический выбор секций и вариантов одним кликом
✨ **Полноэкранный предпросмотр** - просмотр сайта на весь экран для корректного отображения стилей
✨ **Читаемые названия вариантов** - названия хранятся в meta.json
✨ **Дополнительные страницы** - возможность создания внутренних страниц сайта
✨ **Выбор стилей и палитр** - гибкая настройка дизайна
✨ **Автоматическая генерация архива** - готовый ZIP с структурой проекта
✨ **Минимум 9 секций** - обеспечивает полноценность сайта